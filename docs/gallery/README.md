# Gallery

Curated selections of standout renders with links to their experiment logs and source assets.

## Tips

- Include compressed thumbnails in `assets/previews/` and link back here.
- Add context: prompt names, workflow versions, and notable post-processing steps.
- Keep large files in external storage and reference the location instead of committing them.
